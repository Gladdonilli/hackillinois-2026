# LARYNX Frontend Environment Variables
# Copy to .env.local and fill in values

# API endpoint — defaults to direct Modal if not set
# For production: use CF Worker URL
# VITE_API_URL=https://larynx-api.voxlarynx.tech/api/analyze
# For local dev with wrangler:
# VITE_API_URL=http://localhost:8787/api/analyze
