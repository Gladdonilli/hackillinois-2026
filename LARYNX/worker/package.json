{
  "name": "larynx-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute larynx-analysis --local --file=src/schema.sql",
    "db:init:prod": "wrangler d1 execute larynx-analysis --remote --file=src/schema.sql"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241230.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.114.17"
  }
}
